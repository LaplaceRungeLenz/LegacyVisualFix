# 0.2 默认主题设计与实现

用户于 2026-09-12 批准略提亮的科技蓝灰渐变版本。

- 背景：原创 1600×900 椭圆柔光渐变，边缘 RGB(11,21,33)，中心 RGB(21,55,80)，无网格或装饰线。
- Logo：直接使用 GTNH 官方 logo2.png，按原始宽高比显示，默认中心为 (50%,38%)、高度为屏幕 37%。独立配置和独立纹理生命周期。
- 标题：象牙白像素字形，默认 GT NEW HORIZONS，位于屏幕 62% 高度。
- 进度条：默认宽度为屏幕 40%，中心高度 71%，GUI 高度 10 像素；灰色边框与金色分层填充，填充保留内边距。
- 状态：左侧 RELOADING RESOURCES，右侧真实监听器完成数；详细阶段默认关闭，可用 showDetails 开启。
- 字形和条材质使用最近邻采样；背景和官方 Logo 使用线性采样。
- 原创背景与进度条通过 tools/GenerateThemeAssets.java 复现。AI 设计预览不作为运行时图片打包。
- 0.1 原样默认配置按 Properties 全量比较后升级，并保留备份；存在任意自定义键或值时保留配置。

实现保留原有同步资源加载与 GL 状态恢复方式，不更改监听器执行顺序和错误传播。主进度仍表示监听器完成比例，不表示时间比例。
